<% if notice %>
  <div class="notification is-success is-light">
    <%= notice %>
  </div>
<% end %>
<h1>Restaurantes</h1>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Endereço</th>
      <th>Categoria</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @restaurants.each do |restaurant| %>
      <tr>
        <td><%= restaurant.name %></td>
        <td><%= restaurant.address %></td>
        <td><%= restaurant.category.name %></td>
        <td><%= link_to 'Editar', edit_restaurant_path(restaurant), class: 'button is-link' %></td>
        <td><%= link_to 'Excluir', restaurant, method: :delete, class: 'button is-danger', data: { confirm: 'Tem certeza que quer excluir?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Adicionar Restaurante', new_restaurant_path, class: 'button is-primary' %>
